# Grafana file provisioning for the dashboards/ directory (mounted read-only)
apiVersion: 1
providers:
  - name: padwan-llm
    type: file
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /padwan-dashboards
      foldersFromFilesStructure: false
